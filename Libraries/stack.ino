void loop() {
  // put your main code here, to run repeatedly:
  if(phase = 4,5,6){
    Accel = (accelX^2 +accelY^2)^(1/2) 
    if(Accel < TBD){
      reverse_rotating();
      
    }else (){
      break;
    }
  }
}
